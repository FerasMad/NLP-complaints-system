# Data — training-only artifacts not shipped; canary set is the exception
data/
!data/canary/
!data/canary/*

# Trained models — too large for git, fetched from HF Hub at deploy time
models/bakeoff/
models/ensemble/
models/error_analysis/
models/*.pkl
models/*.joblib
models/*.bin
models/*.safetensors
models/*.txt
models/*.png
# But DO track the ensemble manifest + label map (small, critical for runtime)
!models/ensemble_final/
!models/ensemble_final/*.json
!models/single_final/
!models/single_final/*.json
!models/label_map.json

# Local-only docs
PROJECT_HANDOFF.md
team_message.pdf
documentation.pdf

# Python
__pycache__/
*.py[cod]
*.pyo
.env
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Tooling
.claude/

# Pytest
.pytest_cache/
.coverage
htmlcov/

# CI artifacts
*.log

# Gradio cache
.gradio/
