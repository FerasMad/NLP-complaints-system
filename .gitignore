# Data files — raw and processed CSVs excluded; data/text/ is tracked
data/raw/
data/processed/
data/raw/*.csv
data/processed/*.csv

# Trained models — too large for git, fetched from HF Hub at deploy time
models/bakeoff/
models/ensemble/
models/error_analysis/
models/*.pkl
models/*.joblib
models/*.bin
models/*.safetensors
models/*.txt
models/*.png
# But DO track the ensemble manifest (it's small + critical for deploy)
!models/ensemble_final/
!models/ensemble_final/*.json

# Python
__pycache__/
*.py[cod]
*.pyo
.env
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Tooling
.claude/

# Pytest
.pytest_cache/
.coverage
htmlcov/

# CI artifacts
*.log

# Gradio cache
.gradio/
