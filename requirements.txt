# Core inference (deploy + training)
# NOTE for GPU install: use the CUDA wheel index instead of the default PyPI:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.6
transformers>=5.7
datasets>=4.0
accelerate>=1.13
sentencepiece>=0.2
sacremoses>=0.1

# Classical / data
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
joblib>=1.3

# Web service + UI
fastapi>=0.110
uvicorn>=0.29
gradio>=4.0

# Plots + analysis
seaborn>=0.13
matplotlib>=3.8

# Data ingestion
google-play-scraper>=1.2
requests>=2.31

# Optional — Arabic preprocessing (Lana's text-cleaning notebook only).
# camel-tools 1.5+ requires Python <3.13; pin <1.5 if on 3.13.
# camel-tools>=1.4,<1.5

# Tests
pytest>=8.0
